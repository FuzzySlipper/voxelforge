# AGENTS.md (Autogenerated)

> WARNING: This file is autogenerated by `scripts/generate_agents.py`. Do not edit it directly.
> Edit the project-local fragment `agents-project.md`, then regenerate.
> Shared/global agent policy lives in Den guidance documents, not in this file.
>
> Generation sources:
> - Den guidance: `get_agent_guidance(project_id="voxelforge")`
> - local: `agents-project.md`

## Den Guidance Bootstrap

- Project ID: `voxelforge`
- Resolve live guidance with the Den MCP `get_agent_guidance` tool or `den guidance` before substantial work.
- Treat the resolved Den guidance packet and its referenced Den documents as the source of truth.
- If Den is unreachable, stop and tell the user which Den tool or command failed and what you were about to do. Do not reconstruct Den state from local files.
- Use the project-local notes below only as bootstrap context for connecting to Den and working in this repository.

# VoxelForge Local Bootstrap

Project-specific live guidance lives in Den at `[doc: voxelforge/project-bootstrap-guide]`.

Use project ID `voxelforge` for Den tasks, messages, documents, librarian queries, and guidance lookups.

## Local Commands

```bash
git submodule update --init --recursive
./build-native.sh
dotnet build voxelforge.slnx
dotnet test voxelforge.slnx
dotnet run --project src/VoxelForge.Engine.MonoGame
dotnet run --project src/VoxelForge.Engine.MonoGame -- --headless
```
